% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/si_sharmaparton2019.R
\name{si_sharmaparton2019}
\alias{si_sharmaparton2019}
\title{Sharma and Parton (2019) site index model for white pine plantations}
\usage{
si_sharmaparton2019(
  age,
  height = NULL,
  si = NULL,
  base_age = 50,
  total_height = TRUE
)
}
\arguments{
\item{age}{Numeric vector. Breast-height age (years), with \code{age > 0}.}

\item{height}{Optional numeric vector. Stand height (m). If
\code{total_height = TRUE} (default), this is total height; otherwise it is
height above breast height. If provided, \code{si} is predicted.}

\item{si}{Optional numeric vector. Site index (m above breast height) at
\code{base_age} years breast-height age. If provided, \code{height} is predicted.}

\item{base_age}{Positive numeric scalar. Site-index base age (years at
breast height). Defaults to \code{50}.}

\item{total_height}{Logical scalar. If \code{TRUE} (default), interpret input
\code{height} as total height and return predicted \code{height} as total height. If
\code{FALSE}, use the source-paper scale of height above breast height.}
}
\value{
A tibble with columns:
\describe{
\item{height}{Predicted stand height (m), returned when input \code{si} is provided. This is total height when \code{total_height = TRUE}, otherwise height above breast height.}
\item{si}{Predicted site index (m above breast height), returned when input \code{height} is provided.}
}
}
\description{
Implementation of the non-climate-sensitive McDill-Amateis dynamic height
equation fitted by Sharma and Parton (2019) for plantation-grown eastern
white pine (\code{PINU.STR}) in Ontario.
}
\details{
\strong{Species coverage:} \code{PINU.STR}.

\strong{Geographic use:} Ontario white pine plantations.

\strong{Age definition note:} \code{age} is breast-height age (years).

\strong{Height definition note:} the source model uses heights above breast
height (m), not total height. For consistency with other site-index
functions in this package, this implementation defaults to using total
height in the public API (\code{total_height = TRUE}) and converts internally by
subtracting or adding 1.3 m as needed. Set \code{total_height = FALSE} to work on
the original source scale directly.

\strong{Base-age note:} the underlying dynamic equation is base-age
invariant. The source paper illustrates site-index curves using a base age
of 25 years breast-height age. For consistency with other site-index
functions in this package, this implementation instead defaults to a base
age of 50 years breast-height age. Any positive \code{base_age} can be supplied.

Provide exactly one of \code{height} or \code{si}:
\itemize{
\item If \code{height} is provided, the function predicts \code{si}.
\item If \code{si} is provided, the function predicts \code{height}.
}

Inputs and outputs are metric and match the source model scale.

Both directions are explicit closed forms derived from the same dynamic
equation.
}
\examples{
# Predict site index from age + height
si_sharmaparton2019(
  age = c(20, 25, 40),
  height = c(7, 9, 15)
)

# Predict height from age + site index (default base age 50)
si_sharmaparton2019(
  age = c(20, 25, 40),
  si = c(8, 10, 12)
)

# Predict height using an alternative base age
si_sharmaparton2019(
  age = 35,
  si = 12,
  base_age = 25
)

}
\references{
Sharma, M., & Parton, J. (2019). Modelling the effects of climate on site
productivity of white pine plantations. Canadian Journal of Forest
Research, 49, 1289-1297. https://doi.org/10.1139/cjfr-2019-0165
}
