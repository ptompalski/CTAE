% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/si_lafleche2013.R
\name{si_lafleche2013}
\alias{si_lafleche2013}
\title{Lafleche et al. (2013) ecological-site IQS curves for southern Quebec}
\usage{
si_lafleche2013(
  age,
  species,
  ecological_region,
  ecological_type,
  ecological_subregion = NULL,
  curve_set = "potential",
  base_age = 50,
  include_metadata = FALSE
)
}
\arguments{
\item{age}{Numeric vector. Corrected age above 1 m (years), with \code{age > 0}.}

\item{species}{Character vector of NFI species codes
(for example \code{"PICE.GLA"}).}

\item{ecological_region}{Character vector of Quebec ecological-region codes
(French source term: \code{region_ecologique}; for example \code{"4f"}).}

\item{ecological_type}{Character vector of ecological-type codes
(French source term: \code{type_ecologique}; for example \code{"MS22"}).}

\item{ecological_subregion}{Optional character vector of ecological-subregion
codes (French source term: \code{subregion_ecologique}) when needed, for
example \code{"5eS"}. Defaults to \code{NULL}, which is treated as missing.}

\item{curve_set}{Character scalar. One of \code{"potential"} or \code{"observed"}.
Defaults to \code{"potential"}. These map internally to the source-table curve
sets \code{IQSstation} and \code{IQSobserved}, respectively.}

\item{base_age}{Positive numeric scalar. IQS reference age (years above
1 m). Defaults to \code{50}.}

\item{include_metadata}{Logical scalar. If \code{TRUE}, append lookup metadata and
fitted-curve metadata to the returned tibble.}
}
\value{
A tibble with columns:
\describe{
\item{height}{Predicted height (m) at \code{age}.}
\item{si}{Fixed-curve IQS value (m) at \code{base_age}.}
}
When \code{include_metadata = TRUE}, additional columns describing the matched
ecological key and parameter row are included.
}
\description{
Implementation of the fixed ecological-site height curves published by
Lafleche et al. (2013) for major commercial tree species in southern Quebec.
Unlike the polymorphic \verb{si_*} models in this package, these curves are
selected by ecological key and use fixed coefficients for each
species-region-(subregion)-type combination.
}
\details{
\strong{Species coverage:} \code{ABIE.BAL}, \code{BETU.PAP}, \code{PICE.GLA}, \code{PICE.MAR},
\code{PICE.RUB}, \code{PINU.BAN}, \code{PINU.STR}, \code{POPU.GRA}, \code{POPU.TRE}, \code{THUJ.OCC}.

\strong{Geographic use:} ecological types in southern Quebec.

\strong{Age definition note:} \code{age} is corrected age above 1 m height
(years).

\strong{Height definition note:} the source curves predict stand height in
metres, constrained to pass through \verb{(age = 0, height = 1)}.

\strong{Base-age note:} IQS is defined as predicted height at 50 years above
1 m. \code{base_age} defaults to \code{50}, but any positive scalar can be supplied.

This function always predicts height from \code{age}; it also returns the
corresponding fixed-curve IQS value evaluated at \code{base_age}.

\code{curve_set} chooses between the two curve families reported in the source:
\code{"potential"} corresponds to the fitted potential-height curves used to
derive \code{IQSstation} in the report, whereas \code{"observed"} corresponds to the
separate curves fitted to observed height growth (\code{IQSobserved}). In the
source tables these appear as \code{IQSstation} and \code{IQSobserved}, respectively.

At present, \code{CR}, \code{WE}, and \code{LIN} equation forms are implemented directly
from the publication and validated against the published \code{IQSstation}
summaries. The single \code{LOGIST3} row is kept in the parameter data but raises
an error here because its printed equation/coefficients have not yet been
reconciled with the published table values.
}
\examples{
si_lafleche2013(
  age = 50,
  species = "PICE.GLA",
  ecological_region = "2a",
  ecological_type = "MJ11"
)

si_lafleche2013(
  age = c(25, 50),
  species = "ABIE.BAL",
  ecological_region = "3c",
  ecological_type = "MJ12",
  curve_set = "potential",
  include_metadata = TRUE
)

# Vectorized use with the pipe operator
trees_h <- tibble::tibble(
  species = c("ABIE.BAL", "PICE.GLA"),
  age = c(25, 50),
  ecological_region = c("3c", "2a"),
  ecological_type = c("MJ12", "MJ11")
)

trees_h |>
  dplyr::mutate(
    iqs_pred = si_lafleche2013(
      age = age,
      species = species,
      ecological_region = ecological_region,
      ecological_type = ecological_type
    )
  ) |>
  tidyr::unnest(iqs_pred)

}
\references{
Lafleche, V., S. Bernier, J.-P. Saucier, and C. Gagne. 2013. \emph{Indices
de qualite de station des principales essences commerciales en fonction des
types ecologiques du Quebec meridional}. Quebec, ministere des Ressources
naturelles, Direction des inventaires forestiers, 115 p.
}
