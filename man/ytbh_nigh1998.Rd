% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ytbh_nigh1998.R
\name{ytbh_nigh1998}
\alias{ytbh_nigh1998}
\title{Nigh (1998) years-to-breast-height model for interior western hemlock}
\usage{
ytbh_nigh1998(si = NULL, ytbh = NULL)
}
\arguments{
\item{si}{Optional numeric vector. Site index (m, base age 50 years at breast
height). If provided, \code{ytbh} is predicted.}

\item{ytbh}{Optional numeric vector. Years to breast height (years). If
provided, \code{si} is predicted.}
}
\value{
A tibble with a single column:
\describe{
\item{ytbh}{Predicted years to breast height (years), returned when \code{si}
is provided.}
\item{si}{Predicted site index (m), returned when \code{ytbh} is provided.}
}
}
\description{
Implementation of the Nigh (1998) years-to-breast-height (YTBH) model for
western hemlock (\code{TSUG.HET}) in the interior of British Columbia. The
model estimates the number of years a top-height tree takes to grow from
germination to breast height (1.3 m), which is used to convert breast-height
age to total age (and vice versa).
}
\details{
The source equation (eq. 5) is:
\deqn{YTBH = 446.6 \times SI^{-1.432}}

Provide exactly one of \code{si} or \code{ytbh}:
\itemize{
\item If \code{si} is provided, the function predicts \code{ytbh}.
\item If \code{ytbh} is provided, the function predicts \code{si} by inverting the
equation: \eqn{SI = (YTBH / 446.6)^{1 / -1.432}}.
}
}
\examples{
# Predict years-to-breast-height from site index
ytbh_nigh1998(si = c(10, 15, 20))

# Invert: predict site index from years-to-breast-height
ytbh_nigh1998(ytbh = c(8, 4, 3))

}
\references{
Nigh, G.D. (1998). A system for estimating height and site index of western
hemlock in the interior of British Columbia. The Forestry Chronicle 74(4):
588--596.
}
