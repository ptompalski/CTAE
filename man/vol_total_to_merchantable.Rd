% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boudewyn_vtot2vmerch.R
\name{vol_total_to_merchantable}
\alias{vol_total_to_merchantable}
\title{Convert total volume to merchantable volume (Boudewyn et al. 2007; Appendix 6 / Table 14)}
\usage{
vol_total_to_merchantable(
  vol_total,
  species,
  jurisdiction,
  ecozone,
  include_prop = FALSE,
  clamp_prop = TRUE,
  warn_outside = TRUE,
  clamp_x = FALSE
)
}
\arguments{
\item{vol_total}{Numeric vector. Total volume per hectare (all live trees).}

\item{species}{Character vector. NFI species codes (e.g. \code{"PICE.MAR"}). Only genus is used.}

\item{jurisdiction}{Character vector. Jurisdiction code (e.g. \code{"AB"}).}

\item{ecozone}{Numeric vector. Ecozone code (1--15).}

\item{include_prop}{Logical. If TRUE, return the predicted merchantable proportion.}

\item{clamp_prop}{Logical. If TRUE (default), clamp predicted proportion to [0, 1].}

\item{warn_outside}{Logical. If TRUE (default), warn when \code{vol_total < volmin}.}

\item{clamp_x}{Logical. If TRUE, replace \code{vol_total} with \code{pmax(vol_total, volmin)}
before computing the proportion model. Disabled by default.}
}
\value{
A tibble with one row per input and column \code{merchantable_volume}.
  Optionally includes \code{prop_merch} and \code{vol_total_used}.
}
\description{
Uses Boudewyn et al. (2007) Appendix 6 (Table 14) parameters
to convert total volume (all live trees) to merchantable volume via a genus-level model.
}
